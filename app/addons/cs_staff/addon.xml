<?xml version="1.0"?>
<addon scheme="3.0">
    <id>cs_staff</id>
    <status>active</status>
    <version>1.0</version>
    <priority>1000</priority>
    <position>100</position>
    <default_language>ru</default_language>
    <auto_install>MULTIVENDOR,ULTIMATE</auto_install>
    <queries>
        <item>
            CREATE TABLE `?:staff_table` (
                `staff_id` mediumint(8) unsigned NOT NULL auto_increment,
                `firstname` varchar(32) NOT NULL default '',
                `lastname` varchar(32) NOT NULL default '',
                `position` varchar(32) NOT NULL default '',
                `status` char(1) NOT NULL default 'A',
                `creation_date` int(11) unsigned NOT NULL default 0,
                PRIMARY KEY  (`staff_id`),
                KEY `status` (`status`)
            ) Engine=MyISAM DEFAULT CHARSET UTF8;
        </item>
        <item for="install">
            REPLACE INTO ?:seo_names (name, type, dispatch, lang_code) VALUES ('cs_staff', 's', 'cs_staff.view', 'ru');
        </item>
        <item for="uninstall">
            DELETE FROM ?:seo_names WHERE name = 'cs_staff';
        </item>
        <item for="uninstall">
            DROP TABLE IF EXISTS ?:staff_table;
        </item>

    </queries>
</addon>


